"""ASX + Solana Meme Coin Portfolio DashboardSecrets required: ANTHROPIC_API_KEY, ADMIN_PASSWORDOptional:         HELIUS_API_KEY, BIRDEYE_API_KEY"""import streamlit as stimport yfinance as yfimport pandas as pdimport numpy as npimport plotly.graph_objects as gofrom plotly.subplots import make_subplotsfrom datetime import datetime, dateimport anthropicimport jsonimport urllib.parseimport requestsimport warningswarnings.filterwarnings("ignore")# ─────────────────────────────────────────────# PAGE CONFIG# ─────────────────────────────────────────────st.set_page_config(    page_title="Portfolio Dashboard",    page_icon="📈",    layout="wide",    initial_sidebar_state="expanded")# ─────────────────────────────────────────────# THEME────────────────────────────────────────ACTUAL_HOLDINGS = {    "AKN.AX": {"avg_entry": 0.043, "shares": 212782, "name": "AuKing Mining"},    "XST.AX": {"avg_entry": 0.120, "shares": 0,      "name": "Xstate Resources"},}WATCHLIST = {    "G11.AX": {"name": "Group 11 Technologies"},    "VRC.AX": {"name": "Volt Resources"},    "RNX.AX": {"name": "Renegade Exploration"},}AKN_MILESTONES = [    {"stage": "Discovery",  "target_cap_m": 55},    {"stage": "Resource",   "target_cap_m": 100},    {"stage": "Developer",  "target_cap_m": 250},]COMPARISON_START = "2026-01-01"# ─────────────────────────────────────────────# CONSTANTS — CRYPTO# ─────────────────────────────────────────────DEFAULT_TOKENS = {    "ALON": {        "address":   "8XtRWb4uAAJFMP4QQhoYYCWR6X─────────────────────────────────────def signal_badge_html(label):    color = SIGNAL_COLORS.get(label, "#8b949e")    return (f'<span class="signal-badge" style="background:{color}22;color:{color};'            f'border:2px solid {color};">{label}</span>')def pill_html(text, color):    return f'<span class="pill" style="background:{color}22;color:{color};border:1px solid {color};">{text}</span>'def shorten_addr(addr):    return f"{addr[:4]}...{addr[-4:]}" if addr else "—"# ─────────────────────────────────────────────# URL HELPERS# ─────────────────────────────────────────────def encode_holdings(holdings):    payload = {t: {"s": h["shares"], "e": h["avg_entry"], "n": h.get("name", t)}               for t, h in holdings.items()}    return urllib.parse.quote(jso.markdown("<br>", unsafe_allow_html=True)    if st.button("🔄 Refresh", help="Reload latest data"):        st.cache_data.clear()        st.rerun()st.markdown("---")# ─────────────────────────────────────────────# AI UNLOCK CHECK# ─────────────────────────────────────────────admin_pw    = st.secrets.get("ADMIN_PASSWORD", "")typed_pw    = st.session_state.get("refresh_pw", "")ai_unlocked = (not admin_pw) or (typed_pw == admin_pw)# ═══════════════════════════════════════════════# ASX DASHBOARD VIEWS# ═══════════════════════════════════════════════if dashboard_mode == "🇦🇺 ASX Portfolio":    if view == "Portfolio Overview":        st.subheade────────────────────────────────st.markdown("---")st.caption("⚠️ Not financial advice. Meme coins are extremely high risk. "           "ASX data via Yahoo Finance. Crypto data via DexScreener + Helius. "           "AI analysis via Anthropic Claude with web search.")"""ASX + Solana Meme Coin Portfolio DashboardSecrets required: ANTHROPIC_API_KEY, ADMIN_PASSWORDOptional:         HELIUS_API_KEY, BIRDEYE_API_KEY"""import streamlit as stimport yfinance as yfimport pandas as pdimport numpy as npimport plotly.graph_objects as gofrom plotly.subplots import make_subplotsfrom datetime import datetime, dateimport anthropicimport jsonimport urllib.parseimport requestsimport warningswarnings.filterwarnings("ignore")# ─────────────────────────────────────────────# PAGE CONFIG# ─────────────────────────────────────────────st.set_page_config(    page_title="Portfolio Dashboard",    page_icon="📈",    layout="wide",    initial_sidebar_state="expanded")# ─────────────────────────────────────────────# THEME────────────────────────────────────────ACTUAL_HOLDINGS = {    "AKN.AX": {"avg_entry": 0.043, "shares": 212782, "name": "AuKing Mining"},    "XST.AX": {"avg_entry": 0.120, "shares": 0,      "name": "Xstate Resources"},}WATCHLIST = {    "G11.AX": {"name": "Group 11 Technologies"},    "VRC.AX": {"name": "Volt Resources"},    "RNX.AX": {"name": "Renegade Exploration"},}AKN_MILESTONES = [    {"stage": "Discovery",  "target_cap_m": 55},    {"stage": "Resource",   "target_cap_m": 100},    {"stage": "Developer",  "target_cap_m": 250},]COMPARISON_START = "2026-01-01"# ─────────────────────────────────────────────# CONSTANTS — CRYPTO# ─────────────────────────────────────────────DEFAULT_TOKENS = {    "ALON": {        "address":   "8XtRWb4uAAJFMP4QQhoYYCWR6X─────────────────────────────────────def signal_badge_html(label):    color = SIGNAL_COLORS.get(label, "#8b949e")    return (f'<span class="signal-badge" style="background:{color}22;color:{color};'            f'border:2px solid {color};">{label}</span>')def pill_html(text, color):    return f'<span class="pill" style="background:{color}22;color:{color};border:1px solid {color};">{text}</span>'def shorten_addr(addr):    return f"{addr[:4]}...{addr[-4:]}" if addr else "—"# ─────────────────────────────────────────────# URL HELPERS# ─────────────────────────────────────────────def encode_holdings(holdings):    payload = {t: {"s": h["shares"], "e": h["avg_entry"], "n": h.get("name", t)}               for t, h in holdings.items()}    return urllib.parse.quote(jso.markdown("<br>", unsafe_allow_html=True)    if st.button("🔄 Refresh", help="Reload latest data"):        st.cache_data.clear()        st.rerun()st.markdown("---")# ─────────────────────────────────────────────# AI UNLOCK CHECK# ─────────────────────────────────────────────admin_pw    = st.secrets.get("ADMIN_PASSWORD", "")typed_pw    = st.session_state.get("refresh_pw", "")ai_unlocked = (not admin_pw) or (typed_pw == admin_pw)# ═══════════════════════════════════════════════# ASX DASHBOARD VIEWS# ═══════════════════════════════════════════════if dashboard_mode == "🇦🇺 ASX Portfolio":    if view == "Portfolio Overview":        st.subheader="#21262d", showgrid=True, zeroline=False),    legend=dict(bgcolor="#161b22", bordercolor="#30363d", borderwidth=1),    margin=dict(l=0, r=0, t=40, b=0),)# ─────────────────────────────────────────────# HELPERS# ─────────────────────────────────────────────def signal_badge_html(label):    color = SIGNAL_COLORS.get(label, "#8b949e")    return (f'<span class="signal-badge" style="background:{color}22;color:{color};'            f'border:2px solid {color};">{label}</span>')def pill_html(text, color):    return f'<span class="pill" style="background:{color}22;color:{color};border:1px solid {color};">{text}</span>'def shorten_addr(addr):    return f"{addr[:4]}...{addr[-4:]}" if addr else "—"# ─────────────────────────────────────────────# URL HELPERS# ─────────────────────────────────────────────def encode_holdings(holdings):    payload = {t: {"s": h["shares"], "e": h["avg_entry"], "n": h.get("name", t)}               for t, h in holdings.items()}    return urllib.parse.quote(json.dumps(payload, separators=(",", ":")))def decode_holdings(encoded):    try:        payload = json.loads(urllib.parse.unquote(encoded))        return {t: {"shares": v["s"], "avg_entry": v["e"], "name": v.get("n", t)}                for t, v in payload.items()}    except Exception:        return {}# ─────────────────────────────────────────────# SESSION STATE INIT# ─────────────────────────────────────────────def init_state():    if "holdings" not in st.session_state:        url_param = st.query_params.gROPIC CLIENT# ─────────────────────────────────────────────@st.cache_resourcedef get_anthropic_client():    try:        api_key = st.secrets["ANTHROPIC_API_KEY"]        return anthropic.Anthropic(api_key=api_key)    except Exception:        return None# ─────────────────────────────────────────────# DEXSCREENER API# ─────────────────────────────────────────────@st.cache_data(ttl=60)def fetch_dexscreener(token_address):    """Fetch token data from DexScreener."""    try:        url  = f"https://api.dexscreener.com/latest/dex/tokens/{token_address}"        resp = requests.get(url, timeout=10)        data = resp.json()        pairs = data.get("pairs", [])        if not pairs:            return None        # Pick highest liquidity Solana paith col_title:    if dashboard_mode == "🇦🇺 ASX Portfolio":        st.markdown("# 📈 ASX Portfolio Dashboard")    else:        st.markdown("# 🪙 Solana Meme Dashboard")    st.markdown(f"<span style='color:#8b949e'>{date.today().strftime('%A, %d %B %Y')}</span>",                unsafe_allow_html=True)with col_ref:    st.markdown("<br>", unsafe_allow_html=True)    if st.button("🔄 Refresh", help="Reload latest data"):        st.cache_data.clear()        st.rerun()st.markdown("---")# ─────────────────────────────────────────────# AI UNLOCK CHECK# ─────────────────────────────────────────────admin_pw    = st.secrets.get("ADMIN_PASSWORD", "")typed_pw    = st.session_state.get("refresh_pw", "")ai_unlocked = (not admin_pw) or (typed_pw == admin_pw)# ═════════════════════════mport jsonimport urllib.parseimport requestsimport warningswarnings.filterwarnings("ignore")# ─────────────────────────────────────────────# PAGE CONFIG# ─────────────────────────────────────────────st.set_page_config(    page_title="Portfolio Dashboard",    page_icon="📈",    layout="wide",    initial_sidebar_state="expanded")# ─────────────────────────────────────────────# THEME# ─────────────────────────────────────────────st.markdown("""<style>    html, body, [class*="css"] { background-color: #0d1117; color: #e6edf3; font-family: 'Inter', sans-serif; }    .stApp { background-color: #0d1117; }    section[data-testid="stSidebar"] { background-cct(DEFAULT_TOKENS)    for key in ["catalyst_data", "catalyst_error", "catalyst_generated_at"]:        if key not in st.session_state:            st.session_state[key] = Noneinit_state()# ─────────────────────────────────────────────# ANTHROPIC CLIENT# ─────────────────────────────────────────────@st.cache_resourcedef get_anthropic_client():    try:        api_key = st.secrets["ANTHROPIC_API_KEY"]        return anthropic.Anthropic(api_key=api_key)    except Exception:        return None# ─────────────────────────────────────────────# DEXSCREENER API# ─────────────────────────────────────────────@st.cache_data(ttl=60)def fetch_dexscreener(token_addressST')}")# ─────────────────────────────────────────────# HEADER# ─────────────────────────────────────────────col_title, col_ref = st.columns([5, 1])with col_title:    if dashboard_mode == "🇦🇺 ASX Portfolio":        st.markdown("# 📈 ASX Portfolio Dashboard")    else:        st.markdown("# 🪙 Solana Meme Dashboard")    st.markdown(f"<span style='color:#8b949e'>{date.today().strftime('%A, %d %B %Y')}</span>",                unsafe_allow_html=True)with col_ref:    st.markdown("<br>", unsafe_allow_html=True)    if st.button("🔄 Refresh", help="Reload latest data"):        st.cache_data.clear()        st.rerun()st.markdown("---")# ─────────────────────────────────────────────# AI UNLOCK CHECK# ─────────────────────────────────────────────admin_pw    = st.secrets.get("ADMIN_PASSWORD", "")typed_pw    = st.session_state.get("refresh_pw", "")ai_unlocked = (not admin_pw) or (typed_pw == admin_pw)# ═══════════════════════════════════════════════# ASX DASHBOARD VIEWS# ═══════════════════════════════════════════════if dashboard_mode == "🇦🇺 ASX Portfolio":    if view == "Portfolio Overview":        st.subheader("🗂 Actual Holdings")        total_value, total_cost = 0, 0        for ticker, holding in st.session_state.holdings.items():            hist, info = fetch_ticker(ticker)            if hist.empty:                st.warning(f"Could not load {ticker}.")                continue            shares    = holding["shares"]            avg_entry = holding["avg_entolio DashboardSecrets required: ANTHROPIC_API_KEY, ADMIN_PASSWORDOptional:         HELIUS_API_KEY, BIRDEYE_API_KEY"""import streamlit as stimport yfinance as yfimport pandas as pdimport numpy as npimport plotly.graph_objects as gofrom plotly.subplots import make_subplotsfrom datetime import datetime, dateimport anthropicimport jsonimport urllib.parseimport requestsimport warningswarnings.filterwarnings("ignore")# ─────────────────────────────────────────────# PAGE CONFIG# ─────────────────────────────────────────────st.set_page_config(    page_title="Portfolio Dashboard",    page_icon="📈",    layout="wide",    initial_sidebar_state="expanded")# ─────────────────────────────────────────────# THEME# ─────────tml=True)    if st.button("🔄 Refresh", help="Reload latest data"):        st.cache_data.clear()        st.rerun()st.markdown("---")# ─────────────────────────────────────────────# AI UNLOCK CHECK# ─────────────────────────────────────────────admin_pw    = st.secrets.get("ADMIN_PASSWORD", "")typed_pw    = st.session_state.get("refresh_pw", "")ai_unlocked = (not admin_pw) or (typed_pw == admin_pw)# ═══════════════════════════════════════════════# ASX DASHBOARD VIEWS# ═══════════════════════════════════════════════if dashboard_mode == "🇦🇺 ASX Portfolio":    if view == "Portfolio Overview":        st.subheader("🗂 Actual Holdings")      line=False),    legend=dict(bgcolor="#161b22", bordercolor="#30363d", borderwidth=1),    margin=dict(l=0, r=0, t=40, b=0),)# ─────────────────────────────────────────────# HELPERS# ─────────────────────────────────────────────def signal_badge_html(label):    color = SIGNAL_COLORS.get(label, "#8b949e")    return (f'<span class="signal-badge" style="background:{color}22;color:{color};'            f'border:2px solid {color};">{label}</span>')def pill_html(text, color):    return f'<span class="pill" style="background:{color}22;color:{color};border:1px solid {color};">{text}</span>'def shorten_addr(addr):    return f"{addr[:4]}...{addr[-4:]}" if addr else "—"# ─────────────────────────────────────────────# URL HELPERS# ─────────────────────────────────────────────def encode_holdings(holdings):    payload = {t: {"s": h["shares"], "e": h["avg_entry"], "n": h.get("name", t)}               for t, h in holdings.items()}    return urllib.parse.quote(json.dumps(payload, separators=(",", ":")))def decode_holdings(encoded):    try:        payload = json.loads(urllib.parse.unquote(encoded))        return {t: {"shares": v["s"], "avg_entry": v["e"], "name": v.get("n", t)}                for t, v in payload.items()}    except Exception:        return {}# ─────────────────────────────────────────────# SESSION STATE INIT# ─────────────────────────────────────────────def init_state():    if "holdings" not in st.session_state:        url_param = st.query_params.get("portfolio", "")        if u────────────────────────────────────────@st.cache_resourcedef get_anthropic_client():    try:        api_key = st.secrets["ANTHROPIC_API_KEY"]        return anthropic.Anthropic(api_key=api_key)    except Exception:        return None# ─────────────────────────────────────────────# DEXSCREENER API# ─────────────────────────────────────────────@st.cache_data(ttl=60)def fetch_dexscreener(token_address):    """Fetch token data from DexScreener."""    try:        url  = f"https://api.dexscreener.com/latest/dex/tokens/{token_address}"        resp = requests.get(url, timeout=10)        data = resp.json()        pairs = data.get("pairs", [])        if not pairs:            return None        # Pick highest liquidity Solana pair        sol_pairs = [p for p iode == "🇦🇺 ASX Portfolio":        st.markdown("# 📈 ASX Portfolio Dashboard")    else:        st.markdown("# 🪙 Solana Meme Dashboard")    st.markdown(f"<span style='color:#8b949e'>{date.today().strftime('%A, %d %B %Y')}</span>",                unsafe_allow_html=True)with col_ref:    st.markdown("<br>", unsafe_allow_html=True)    if st.button("🔄 Refresh", help="Reload latest data"):        st.cache_data.clear()        st.rerun()st.markdown("---")# ─────────────────────────────────────────────# AI UNLOCK CHECK# ─────────────────────────────────────────────admin_pw    = st.secrets.get("ADMIN_PASSWORD", "")typed_pw    = st.session_state.get("refresh_pw", "")ai_unlocked = (not admin_pw) or (typed_pw == admin_pw)# ════════════════════════════════════mport requestsimport warningswarnings.filterwarnings("ignore")# ─────────────────────────────────────────────# PAGE CONFIG# ─────────────────────────────────────────────st.set_page_config(    page_title="Portfolio Dashboard",    page_icon="📈",    layout="wide",    initial_sidebar_state="expanded")# ─────────────────────────────────────────────# THEME# ─────────────────────────────────────────────st.markdown("""<style>    html, body, [class*="css"] { background-color: #0d1117; color: #e6edf3; font-family: 'Inter', sans-serif; }    .stApp { background-color: #0d1117; }    section[data-testid="stSidebar"] { background-color: #161b22; border-right: 1pxackground:{color}22;color:{color};border:1px solid {color};">{text}</span>'def shorten_addr(addr):    return f"{addr[:4]}...{addr[-4:]}" if addr else "—"# ─────────────────────────────────────────────# URL HELPERS# ─────────────────────────────────────────────def encode_holdings(holdings):    payload = {t: {"s": h["shares"], "e": h["avg_entry"], "n": h.get("name", t)}               for t, h in holdings.items()}    return urllib.parse.quote(json.dumps(payload, separators=(",", ":")))def decode_holdings(encoded):    try:        payload = json.loads(urllib.parse.unquote(encoded))        return {t: {"shares": v["s"], "avg_entry": v["e"], "name": v.get("n", t)}                for t, v in payload.items()}    except Exception:        return {}# ─────────────────────────────n ["catalyst_data", "catalyst_error", "catalyst_generated_at"]:        if key not in st.session_state:            st.session_state[key] = Noneinit_state()# ─────────────────────────────────────────────# ANTHROPIC CLIENT# ─────────────────────────────────────────────@st.cache_resourcedef get_anthropic_client():    try:        api_key = st.secrets["ANTHROPIC_API_KEY"]        return anthropic.Anthropic(api_key=api_key)    except Exception:        return None# ─────────────────────────────────────────────# DEXSCREENER API# ─────────────────────────────────────────────@st.cache_data(ttl=60)def fetch_dexscreener(token_address):    """Fetch token data from ──────────────────────────────────────# HEADER# ─────────────────────────────────────────────col_title, col_ref = st.columns([5, 1])with col_title:    if dashboard_mode == "🇦🇺 ASX Portfolio":        st.markdown("# 📈 ASX Portfolio Dashboard")    else:        st.markdown("# 🪙 Solana Meme Dashboard")    st.markdown(f"<span style='color:#8b949e'>{date.today().strftime('%A, %d %B %Y')}</span>",                unsafe_allow_html=True)with col_ref:    st.markdown("<br>", unsafe_allow_html=True)    if st.button("🔄 Refresh", help="Reload latest data"):        st.cache_data.clear()        st.rerun()st.markdown("---")# ─────────────────────────────────────────────# AI UNLOCK CHECK# ───────────────────────ly — high concentration risk</span>',                                unsafe_allow_html=True)                else:                    st.markdown(f'<span class="safe-flag">✅ Top 3 holders: {top3_pct:.1f}% — reasonable distribution</span>',                                unsafe_allow_html=True)            elif h_err == "no_key":                st.caption("Add HELIUS_API_KEY to secrets for holder data.")            st.markdown("---")# ─────────────────────────────────────────────# FOOTER# ─────────────────────────────────────────────st.markdown("---")st.caption("⚠️ Not financial advice. Meme coins are extremely high risk. "           "ASX data via Yahoo Finance. Crypto data via DexScreener + Helius. "           "AI analysis via Anthropic Claude with web search.")shss